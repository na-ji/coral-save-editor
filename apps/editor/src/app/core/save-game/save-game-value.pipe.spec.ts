import { SaveGameValuePipe } from './save-game-value.pipe';

describe('SaveGameValuePipe', () => {
  it('create an instance', () => {
    const pipe = new SaveGameValuePipe();
    expect(pipe).toBeTruthy();
  });
});
